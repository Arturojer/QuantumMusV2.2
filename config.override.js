/**
 * Override opcional de la URL del servidor (Quantum Mus)
 *
 * - En local: no hace falta cambiar nada; se usa el mismo origen (localhost).
 * - En producción: si tu backend está en otra URL, descomenta la línea siguiente
 *   y sustituye por la dirección real de tu API/servidor (sin barra final).
 *
 * Ejemplo backend en el mismo dominio: no descomentes nada.
 * Ejemplo backend en otro dominio:
 *   window.QUANTUM_MUS_SERVER_URL = 'https://api.midominio.com';
 */
// window.QUANTUM_MUS_SERVER_URL = 'https://tu-servidor-backend.com';
